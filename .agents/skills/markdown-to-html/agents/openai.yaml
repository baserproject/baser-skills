interface:
  display_name: "Markdown to HTML"
  short_description: "Markdown を HTML に変換する軽量スキル"
  brand_color: "#2563EB"
  default_prompt: "Use $markdown-to-html to convert this Markdown into clean HTML."

policy:
  allow_implicit_invocation: true
